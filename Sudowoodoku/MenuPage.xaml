<Page
    x:Class="Sudowoodoku.MenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sudowoodoku"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Grid Background="Black">
		<Viewbox>
			<StackPanel Width="350" Height="350" Padding="25,0,25,0">
				<TextBlock Text="sudokoowdoku" TextWrapping="Wrap" Margin="0,115,0,0" HorizontalAlignment="Center"/>
				<TextBox ElementSoundMode="FocusOnly" x:Name="seedInput" Margin="0,10,0,0" PlaceholderText="level number" InputScope="Number" IsTextPredictionEnabled="False"/>
				<Button ElementSoundMode="Default" Content="get the party started" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,10,0,0" Click="Button_Click"/>
				<Button ElementSoundMode="Default" x:Name="xboxSignInButton" Content="sign into Xbox Live" FontSize="10" HorizontalAlignment="Center" Margin="0,57,0,0" Background="#FF079C00" Width="180" Click="xboxSignInButton_Click" Foreground="White" Visibility="Collapsed" IsEnabled="False"/>
			</StackPanel>
		</Viewbox>
	</Grid>
</Page>
