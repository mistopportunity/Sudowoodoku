<Page
    x:Class="Sudowoodoku.MenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sudowoodoku"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Grid Background="Black">
		<Viewbox>
			<Grid Width="350" Height="350" Padding="25,0,25,0">
				<StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
					<TextBlock Text="sudowoodoku" TextWrapping="Wrap" HorizontalAlignment="Center"/>
					<TextBox PreventKeyboardDisplayOnProgrammaticFocus="True" ElementSoundMode="FocusOnly" x:Name="seedInput" Margin="0,10,0,0" PlaceholderText="level number" InputScope="Number" IsTextPredictionEnabled="False" KeyUp="seedInput_KeyUp"/>
					<Button x:Name="StartButton" ElementSoundMode="Default" Content="get the party started" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,10,0,0" Click="Button_Click"/>
					<Button ElementSoundMode="Default" x:Name="xboxSignInButton" Content="sign into Xbox Live" FontSize="10" HorizontalAlignment="Center" Margin="0,40,0,0" Background="#FF079C00" Width="180" Click="xboxSignInButton_Click" Foreground="White" Visibility="Visible" IsEnabled="true"/>
				</StackPanel>
			</Grid>
		</Viewbox>
	</Grid>
</Page>
